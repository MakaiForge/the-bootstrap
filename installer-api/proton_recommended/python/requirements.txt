# ProtonForge API - Dependências Python
# Python 3.10+
# Instalar com: pip install -r requirements.txt

# Sem dependências externas por enquanto.
# A API usa apenas a biblioteca padrão Python 3.10+:
# - json (manipulação de dados)
# - os, sys, subprocess, shutil (sistema de arquivos)
# - threading (concorrência)
# - sqlite3 (catálogo de jogos)
# - shlex (formatação de comandos)

# Futuras dependências (se necessário):
# ijson    → parse incremental de matched.json (1.5GB+)
# psutil   → monitoramento de processos
